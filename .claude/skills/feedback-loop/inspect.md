# Inspect

Query DOM, accessibility tree, computed styles, and in-page state.

Command (a11y tree): `agent-browser snapshot`
Command (HTML of selector): `agent-browser get html '<selector>'`
Command (text of selector): `agent-browser get text '<selector>'`
Command (computed styles): `agent-browser get styles '<selector>'`
Availability check: `agent-browser --version`
